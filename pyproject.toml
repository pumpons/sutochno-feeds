[project]
name = "sutochno-feeds"
version = "0.1.0"
description = "Custom feed builder for sutochno.ru source feed"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.32",
    "pyyaml>=6.0",
    "jinja2>=3.1",
    "playwright>=1.47",
]

[tool.setuptools]
packages = ["src"]
